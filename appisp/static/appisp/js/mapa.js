import { initializeMap } from './mapa-main.js';

// Garante que o DOM esteja completamente carregado antes de executar o script do mapa.
document.addEventListener('DOMContentLoaded', () => {
    initializeMap();
});